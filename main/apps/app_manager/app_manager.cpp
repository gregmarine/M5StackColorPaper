#include "app_manager.h"

/*
 * No-op: this MVP doesn't have a menu/app-manager, so there's nothing to
 * track a "refresh in progress" flag for. See app_manager.h for context.
 */
void app_manager_set_refresh_in_progress(bool in_progress) {}
